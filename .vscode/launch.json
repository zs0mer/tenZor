{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "make build",
            "name": "Launch compiled app",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Demo/targets/out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
